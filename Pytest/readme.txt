TO RUN:
From parent directory, use the command `pytest tests` to run pytest against the `tests` directory.
It will automatically detect all modules that begin with `test_` and run all functions that begin
with `test_`.